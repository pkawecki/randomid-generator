The script enables user to retrieve random string of given length:

#Imports
const randomID = require("@przemo41/randomid-generator");

#Use
console.log(randomID(10)); - displays 10 character string using default randomizer.
